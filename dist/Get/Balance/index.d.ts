/**
 * 잔액을 가져옵니다
 *
 * @since v1.0.0
 * @param address 주소
 * @param storage 경로
 * @returns bigint
 */
export declare function getBalance(
/**
 * 주소
 */
address: string, 
/**
 * 경로
 */
storage: string): bigint;
